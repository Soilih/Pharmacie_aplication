<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.wonono">

    <uses-permission android:name="android.permission.CALL_PHONE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:name=".MainActivity"
            android:label="Wonono_Aplication">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Espace_Client"
            android:label="Espace Client " />
        <activity
            android:name=".Accueil"
            android:label="Liste des Pharmacies" />
        <activity
            android:name=".Update"
            android:label="Mise à jour des Pharmacies" />
        <activity
            android:name=".Delete"
            android:label="Delete Pharmacie " />
        <activity
            android:name=".Horaire"
            android:label="Disponibiles des pharmacies" />
        <activity
            android:name=".Contact"
            android:label="Gestion des Annuaires  " />

        <activity
            android:name=".Admin"
            android:label="Enregistrement Phramacie " />

        <activity android:name=".Propos" android:label="A propos "></activity>
    </application>

</manifest>